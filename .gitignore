# وابستگی‌ها
node_modules/

# بیلدها
dist/
.turbo/

# فایل‌های محیطی حساس
.env
.env.local
.env.production

# دیتابیس محلی
*.db
apps/server/prisma/dev.db

# فایل‌های قفل اضافی
package-lock.json

# فایل‌های آرشیو و موقت
*.rar
*.zip
*.log

# Founder-supplied recipe import source packages (unzipped inputs — generally not committed; imported via scripts)
# EXCEPTION: garnish_recipe_international_core_150_draft_candidate_v0_6_0/ IS intentionally committed
# (force-tracked) as the reproducible validator source — see data/README.md. Future drafts stay ignored.
/garnish_recipe_*_draft_candidate_*/
# Design reference (read-only visual target — never imported or bundled)
design-reference/

# Founder design handoff bundle (read-only visual target — never imported/bundled)
home-screen-design-exploration/
