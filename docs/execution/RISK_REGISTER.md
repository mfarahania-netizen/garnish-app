# RISK REGISTER

> Execution artifact per **Constitution v1.0.1 — A1.5**. Append-only log of project risks.
> Owner roles: F=Founder · PS=Product Strategist · UX=Designer · AA=AI Architect · BA=Data/Behavior Architect · EL=Engineering Lead · CM=Content Manager · ADV=Legal/Compliance Advisor.
> Probability / Impact scale: Low / Med / High. Status: Open / Mitigating / Closed / Accepted.

| Risk ID | Risk | Area | Probability | Impact | Owner | Mitigation | Trigger | Status |
|---------|------|------|-------------|--------|-------|------------|---------|--------|
| R1 | Leak / reuse of an old secret (Gemini key, JWT) committed to git history | Security | High | High | EL | E1: revoke+rotate, `git rm --cached .env`, history purge (Founder-gated), gitleaks pre-commit + CI | Any secret found by gitleaks/trufflehog | Open |
| R2 | Facilitator rejection / non-response (visa path) | Growth/Visa | Med | High | F | A1.2: start outreach W1/W2, target list of 10 + top-5, alternate path (v2-Phase3) | No reply after 2 follow-ups | Open |
| R3 | AI cost overrun (per-user inference) | AI/Cost | Med | High | AA | E47 Cost Controller v1 (in-memory per-call 8000 / per-user 200000 token caps) + **A10A** persisted cost/usage ledger + **A10B** persisted per-user DAILY budget (enforced before live calls; fail-closed) + **A10C** versioned source-tagged rate catalog (empty until verified rates) + DB-backed `AiSpendAlert` (daily token-threshold alerts; cost-threshold inactive until rates). **Closure needs:** VERIFIED per-model USD rates → real estimated spend + cost-spend alerting/blocking + transactional quota reservation (TOCTOU hardening). | Cost/user above threshold in sandbox | **Mitigating (A10C 2026-06-14)** — ledger + persisted daily budget + rate-catalog scaffold + spend-alert foundation; **no verified rates** (estimatedCost null, cost-alert inactive) + TOCTOU race → not closed. See `docs/execution/E47_A10C_AI_COST_RATE_CATALOG_AND_SPEND_ALERTING_REPORT.md` |
| R4 | Unsafe AI answer (hallucinated nutrition / health claim) | AI/Safety | Med | High | AA | E47 Safety Guard v1 + Nutrition Claim Guard + Prompt-Injection Guard + **A11A** deterministic OUTPUT safety/quality eval harness + **A11B** 54-case EN+Farsi regression corpus + corpus secret/PII validation + **CI-safe offline regression gate** (`ai:eval:regression`; exits non-zero on unsafe-pass / over-block). **Closure needs:** at-scale LIVE output eval + novel-evasion (fuzz/red-team) robustness + continued corpus growth (offline gate uses the stub). | Eval unsafe-rate ≥ 0.1% | **Mitigating (A11B 2026-06-14)** — deterministic output evaluator + durable CI regression gate (both fail directions proven, EN+Farsi); offline-stub default + known-pattern-only (not novel-evasion) → not closed. See `docs/execution/E47_A11B_LARGER_REGRESSION_CORPUS_AND_CONTINUOUS_EVAL_GATE_REPORT.md` |
| R5 | Legal/operational exposure of the Iran sandbox | Compliance | Med | High | ADV | E49: legal opinion + self-host path ready, sandbox = no revenue / no market claim | New legal signal / regulatory change | Open |
| R6 | Data import mismatch (122 recipes / 1008 ingredients) | Data | Med | Med | EL/CM | E9/E10/E11 idempotent importers + resolver coverage ≥98% | Import run diff non-zero | Open |
| R7 | Low onboarding completion | Product | Med | High | PS | Food DNA (E22′): ≤5 min, 15 steps, completion ≥70% gate | Completion < 70% in test | Open |
| R8 | Low D7 retention | Product | Med | High | PS/BA | Briefing + INE + Engagement v1, measured in sandbox (G2: D7 ≥20%) | D7 below gate in cohort | Open |
| R9 | Translation / EU content gap (universalization) | Content | Med | Med | CM | E46: 150–250 EN recipes with gate validation, EN-first eval | EN content fails gate | Open |
| R10 | Team execution drift (3-person team, broad scope) | Execution | High | High | F | Gates, Part 2.3 Do-Not-Build, RACI, "no next Wave before previous is green" | Wave slips / scope creep | Open |
| R11 | Overbuilding in Community | Scope | Med | High | F | Community stage-gated C0–C6, C0 docs only now, gates per Part 2.2 | Build beyond current C-stage | Open |
| R12 | B2B distraction | Scope | Med | Med | F | B2B = governance only (B0) until Year 2, 2 LOI gate for B1 | B-work beyond B0 pre-Y2 | Open |
| R13 | Overengineering in WAT | Scope | Med | Med | EL | WAT W0 spec only, W1 conditional on time-log evidence, permanent deny-list | WAT build beyond W0 pre-evidence | Open |
| R14 | Nutrition source gap | Data/Content | Med | Med | CM | E12 three-tier nutrition policy, 200 source-locked, no number without badge | Recipe shows nutrition w/o source | Open |
| R15 | GDPR / consent failure | Compliance | Med | High | ADV | E4 consent gate (zero pre-consent events), E39 erasure/export, E40 AI-Act memo | Any event before consent | Open |
| R16 | GDPR erasure structurally broken — **audit FAIL (2026-06-13)**: `deleteUser` is a bare `prisma.user.delete()` (no txn/cleanup); **4 relations Restrict** (UserPreference/UserSession/UserEvent/UserBehaviorProfile → deletion throws) and **12 models carry `userId` with NO FK** (orphan leak incl. UserHealthSnapshot/UserBehaviorSignal/snapshots); **no export endpoint**; **no retention/prune cron**. AICallLog SetNull tombstone is correct. | Data/Compliance | High | High | EL/ADV | **E39-1-ERASURE-FIX-ADDITIVE** (proposed, gated): additive FK on the 12 orphans + flip 4 Restrict→Cascade + explicit Recipe.author SetNull + transactional erasure service + erasure audit event + `GET /users/me/export` + retention crons + e2e erasure/export tests. Full matrix in `docs/security/E39_R16_ERASURE_COVERAGE_AUDIT.md`. | **before beta/sandbox real users · before G1 · before external diligence** — erasure throws / orphan rows / no export | **BASELINE_CLOSED for dev/beta (2026-06-14 · E39-FINAL-PRIVACY-GATE).** Closure scope = current dev/beta privacy baseline. Erasure (E39-1C) + export (E39-1D) + retention dry-run policy (E39-1E) + legacy cron guard (E39-1E-1) implemented, merged, and re-verified at the final gate: FK model = 36 User FKs (30 Cascade / 6 SetNull / **0 Restrict** / **0 orphan userId**); **47/47** targeted unit tests; disposable-DB checks erasure **35/35**, export **12/12**, retention **13/13** (NOTHING deleted); no unguarded destructive path remains. **Controlled destructive prune NOT implemented — explicitly DEFERRED** to a future approved operational task (not a G1 blocker per the gate decision). Report: `docs/security/E39_FINAL_PRIVACY_GATE_REPORT.md`. |
| R17 | Missing `RecipeContext` — 4 live files import a non-existent `context/RecipeContext`; components throw at runtime | Frontend/Quality | High | Med | EL | Create the context or fix imports before the affected surfaces ship | mounting shopping/meal-planner/ai-chat surfaces crashes | Open (audit 2026-06-13) |
| R18 | Over-exposed ops/diagnostics endpoints — `recommendation` (build-snapshots, run-signal-detector, build-identity, debug-features, test-penalty, embedding) and root-mounted `diagnostics` (governance, report, metrics, …) require only `AuthGuard`, so any logged-in user can trigger jobs / read system internals | Security | Med | High | EL/PS | Gate `⚠ needs PS/F` routes behind `RolesGuard + @Roles('admin')` after PS/F confirms intended access; move diagnostics under a non-root prefix (see `docs/security/RBAC_ROUTE_MATRIX.md`) | any non-admin user hits an ops/governance route | Open (E3-0 2026-06-13) |
| R19 | 4 pre-existing failing unit specs (`recipes.controller.spec.ts`/`recipes.service.spec.ts` DI errors, `ranking.service.spec.ts:192` stale assertion, `feature-store.service.spec.ts` incomplete mock) — 44/48 pass | Quality/CI | High | Med | EL | CI `test` step is `continue-on-error` (non-blocking) until a dedicated fix ticket lands; then make it a blocking gate | test gate stays red | Open (E6 2026-06-13) |
| R20 | Server lint ~3230 errors (almost all `prettier/prettier` formatting/CRLF; ~2218 auto-fixable) | Quality/CI | Med | Low | EL | CI `lint` step is `continue-on-error` until a `prettier --write` format pass + CRLF/EOL policy lands; then make blocking | lint gate stays red | Open (E6 2026-06-13) |
| R-E39-LEGACY-RETENTION-CRON | Pre-existing **unguarded destructive retention cron** — `apps/server/src/governance/data-retention.service.ts` runs `@Cron('0 0 1 * *')` → `userEvent.deleteMany({ where: { timestamp: { lt: now-1y } } })` on the 1st of every month, in every environment, with NO dry-run / NO env guard / NO approval (active: `ScheduleModule` + `GovernanceModule` wired in `AppModule`). Contradicts the audit's "no retention cron" finding and the E39-1E dry-run-first policy; dual-deletion path on `userEvent`. | Data/Compliance | High | High (data loss) | EL/ADV | Guard behind `RETENTION_DESTRUCTIVE_ENABLED` (default false) or deprecate and migrate to the policy-driven E39-1E flow; add structured count-before-delete logging. Ready patch in `docs/security/E39_1E_RETENTION_CRON_POLICY_REPORT.md`. Out of E39-1E allowed-files scope → flagged, not modified. | next monthly cron tick deletes events with no guard | **MITIGATED (E39-1E-1, 2026-06-13)** — cron neutralized: direct `deleteMany` removed, now delegates to `RetentionService` (dry-run default, deletes nothing; destructive mode refuses via `executeRetention`). Default-safe in all envs; gated on `RETENTION_DESTRUCTIVE_ENABLED`. 4 guard tests green. Destructive prune still not implemented → controlled execution remains a future approved step. |
| R-E1-HISTORY-DEAD-SECRETS | `apps/server/.env` (Gemini/JWT/DB secrets) remains in git history (2 commits: e715471, a65ec03). Values are now DEAD after rotation; repo is private | Security/Compliance | Low (post-rotation) | Med | Founder/EL | **Active risk mitigated:** keys rotated (Gemini revoked/replaced, JWT rotated, DB rotated/replaced), repo made private, `.env` untracked on tip, backup bundle verified. **Purge pending** real Python 3 + git-filter-repo + gitleaks + trufflehog (see `docs/security/E1_SECRET_INCIDENT_STATUS.md`) | before external diligence · before adding collaborators · before making repo public · before G1 security closeout | Mitigated (active) / purge pending (2026-06-13) |

## How to use
- Add a new row whenever a risk is identified; never delete — set Status to `Closed` / `Accepted` with a dated note below.
- Each `Mitigation` should reference an Epic ID or Gate where the control lives.
- Reviewed every **Friday** as part of the Gate Review mini-check (A1.1 rule 7).

## Change history
- 2026-06-13 — Seeded with 15 initial risks per A1.5.
- 2026-06-13 — Added R16 (GDPR erasure structurally broken) and R17 (missing RecipeContext) from the structure/design audit (`docs/audit/STRUCTURE_AND_DESIGN_AUDIT.md`).
- 2026-06-13 — Added R18 (over-exposed ops/diagnostics endpoints) from E3-0 RBAC matrix.
- 2026-06-13 — Added R19 (4 failing specs) and R20 (lint/format debt) from E6 CI setup; both gated non-blocking in CI.
- 2026-06-13 — Added R-E1-HISTORY-DEAD-SECRETS: E1 active exposure mitigated (keys rotated + repo private); history purge deferred (tooling). See `docs/security/E1_SECRET_INCIDENT_STATUS.md`.
- 2026-06-13 — R16 re-assessed via the E39/R16 erasure coverage audit (verdict FAIL): 4 Restrict relations block deletion, 12 orphan `userId` models, no export endpoint, no retention cron. Status kept OPEN; fix proposed as `E39-1-ERASURE-FIX-ADDITIVE`. Full audit: `docs/security/E39_R16_ERASURE_COVERAGE_AUDIT.md`.
- 2026-06-13 — **E39-1A schema repair APPLIED** (migration `20260613170000_e39_1a_erasure_fk_cascade_repair`): 12 orphan models given `User` FK (Cascade), 4 Restrict→Cascade, Recipe.author explicit SetNull; preflight 0 orphans; DB proof = 0 Restrict/No-Action FKs to User. Deletion no longer structurally blocked. R16 stays OPEN pending E39-1B (erasure service + export + retention + ConsentLog/UserAuditLog tombstone). See `docs/security/E39_1A_SCHEMA_REPAIR_REPORT.md`.
- 2026-06-13 — **E39-1B audit-long tombstone foundation APPLIED** (migration `20260613180000_e39_1b_audit_long_tombstone_foundation`): ConsentLog + UserAuditLog → SetNull (survive de-linked); new PII-free `ErasureEvent` proof ledger (SetNull + subjectHash) + foundation `ErasureAuditService` (5 tests). DB proof = 0 blocking FKs; audit-long records survive. R16 stays OPEN pending E39-1C (transactional erasure service + residual-PII scrub + export + retention). See `docs/security/E39_1B_AUDIT_LONG_TOMBSTONE_FOUNDATION_REPORT.md`.
- 2026-06-13 — **E39-1C transactional erasure service IMPLEMENTED** (no migration — residual-PII columns already nullable): `ErasureService.eraseUser` replaces the bare `deleteUser` delete; one `$transaction` revokes sessions, scrubs residual PII (ConsentLog.ip; UserAuditLog.ip/userAgent/details; DataAccessLog.ip/details), writes a PII-free `ErasureEvent`, then deletes the user (Cascade + SetNull). `UsersService.deleteUser` delegates; `DELETE /users/me` returns a PII-free message. 7 targeted tests green; build green. Disposable-DB integration verified (35/35); **merged to master** (commit bc624497). R16 stays OPEN pending E39-1D (`GET /users/me/export`) + E39-1E (retention crons). See `docs/security/E39_1C_TRANSACTIONAL_ERASURE_SERVICE_REPORT.md`.
- 2026-06-13 — **E39-1D GDPR user export endpoint IMPLEMENTED** (no migration — read-only): `GET /users/me/export` (JWT-guarded, current-user-only via `req.user.userId`) → `UserExportService.exportUser`. Stable v1 JSON envelope with 33 section keys covering every user-linked model; recursive secret-key sanitizer + allow-list profile + AICallLog safe-select (no `errorMessage`); per-section limits/fault-isolation. 16 unit tests + disposable-DB integration (12 checks, no secret/other-user leak) green; build green; adversarially reviewed and hardened. R16 stays OPEN pending **E39-1E retention crons** only. See `docs/security/E39_1D_GDPR_USER_EXPORT_ENDPOINT_REPORT.md`.
- 2026-06-13 — **E39-1E retention policy + dry-run foundation IMPLEMENTED** (no migration — count-only): `src/retention/` policy classifies all 50 models; `RetentionService.previewRetention` is count-only (never deletes), `executeRetention` double-guarded (no deletion path). 14 unit + 13 disposable-DB checks green; build green; NOTHING deleted. Adversarial review reclassified `preferenceHistory` → user_owned_active (false-prune fix). **Added R-E39-LEGACY-RETENTION-CRON** (pre-existing unguarded destructive `userEvent` cron discovered) — **R16 stays OPEN** until that cron is guarded/deprecated and a controlled prune is approved. See `docs/security/E39_1E_RETENTION_CRON_POLICY_REPORT.md`.
- 2026-06-13 — **E39-1E-1 legacy retention cron GUARDED** (R-E39-LEGACY-RETENTION-CRON → MITIGATED): `governance/data-retention.service.ts` direct `userEvent.deleteMany` REMOVED; cron now delegates to `RetentionService` — dry-run (deletes nothing) by default, destructive mode (`RETENTION_DESTRUCTIVE_ENABLED=true`) routed through `executeRetention()` which refuses (no deletion path). Default-safe in all environments; one retention code path; no DI rewiring. 4 guard tests + 14 retention unit + 13 disposable green; build green; no data deleted. **R16 stays OPEN** until an approved controlled-prune execution. See `docs/security/E39_1E_RETENTION_CRON_POLICY_REPORT.md`.
- 2026-06-14 — **R16 → BASELINE_CLOSED (E39-FINAL-PRIVACY-GATE).** Final privacy baseline gate run on a dedicated branch (`exec/e39-final-privacy-gate`) against real repo + local/dev `garnish_db`. Verified: FK model = 36 User FKs (30 Cascade / 6 SetNull / 0 Restrict / 0 orphan `userId`); erasure/export/retention re-run green — **47/47** targeted unit tests, disposable-DB checks **erasure 35/35 · export 12/12 · retention 13/13** (NOTHING deleted); legacy cron neutralized; no unguarded destructive path (every production `deleteMany`/`delete` classified as erasure / user-initiated self-scoped / replace-set / in-memory). Build green (both apps). One narrow test-only fix (erasure spec constructor arity after E39-1D). No real data deleted, no destructive prune, no UI/live-Gemini/re-import. **Controlled destructive prune remains explicitly DEFERRED** (future approved operational task; not a G1 blocker per the gate decision). Verdict `E39_BASELINE_PRIVACY_GATE_PASS_R16_CLOSED_WITH_CONTROLLED_PRUNE_DEFERRED`. Report: `docs/security/E39_FINAL_PRIVACY_GATE_REPORT.md`.
- 2026-06-14 — **R-E39-LEGACY-RETENTION-CRON** confirmed MITIGATED at the final gate (cron delegates to dry-run; destructive path refuses; 0 deletions). Remaining future work = a controlled, approved prune-execution policy (tracked as the deferred item under R16). Unchanged: **R-E1-HISTORY-DEAD-SECRETS** (purge still pending, out of E39 scope), **R19/R20** (CI legacy debt, non-blocking).
- 2026-06-14 — **R3 stays Mitigating (E47-A10C rate catalog + spend alerts).** Added a versioned, source-tagged cost-rate catalog (`ai-cost-rate-catalog.ts`, **empty production rates** — no invented prices → `estimatedCostUsd` stays null) + a DB-backed `AiSpendAlert` foundation (additive migration `20260614010000`, FK SetNull) with deduped per-user daily token-threshold alerts (cost-threshold inactive until verified rates). Orchestrator computes cost via the catalog and evaluates alerts after live calls (best-effort, default-skip). AI suite 130/130 (+17); erasure disposable re-verified **36/36** (37 User FKs: 30 Cascade / 7 SetNull / 0 Restrict; AiSpendAlert survives SetNull); build green; no secret. **Not closed** — verified rates + cost-spend alerting + race-proof reservation pending. **R4 unchanged (OPEN).** See `docs/execution/E47_A10C_AI_COST_RATE_CATALOG_AND_SPEND_ALERTING_REPORT.md`.
- 2026-06-14 — **R3 stays Mitigating (E47-A10B persisted daily user budget).** New `PersistedDailyBudgetService` sums real-provider tokens per user per UTC day from the A10A `AICallLog` ledger; the Orchestrator enforces it **before any live provider call** (step 4.5), fails **CLOSED** on lookup error, and is skipped entirely on the default stub path. No schema migration (reused A10A fields). AI suite 113/113 (+13 budget tests); build green; direct-Gemini provider-only; no secret. **Not closed** — per-model USD rates, spend alerting, and transactional (race-proof) reservation still pending; **R4 unchanged (OPEN)**. See `docs/execution/E47_A10B_PERSISTED_DAILY_USER_BUDGET_REPORT.md`.
- 2026-06-14 — **R3 → Mitigating (E47-A10A persisted AI cost ledger).** Extended `AICallLog` (additive migration `20260614000000`) with durable per-call usage/cost fields (`totalTokens`, `usageSource` provider|estimated|unavailable, `costIsEstimated`, `currency`, `costSchemaVersion`); orchestrator writes them on every terminal path; new `ai-cost-policy.ts` (single source of truth; empty rate table → `estimatedCost` null, no faked precision). AI suite 100/100; erasure disposable verify 35/35 (AICallLog SetNull, deletion not blocked); live probe confirmed `usageSource=provider` end-to-end. **R3 stays open-for-closure** (per-model rates + persisted per-user daily budget + alerting deferred). **R4 unchanged (OPEN).** See `docs/execution/E47_A10A_PERSISTED_AI_COST_LEDGER_REPORT.md`.
- 2026-06-14 — **E47-A7/A8/A9 AI live track** (controlled live Gemini smoke PASS · controlled live chat adapter · runtime-boundary gate). **R4 (unsafe AI answer) and R3 (AI cost overrun) remain OPEN** and are the governing risks for any *product* rollout of live AI/chat. Clarification from the A9 gate: live Gemini is **gated/dev-only behind explicit env flags** (default = deterministic/stub); guard order intact (snapshot→injection→cost→safety→provider→nutrition→AICallLog); exactly 4 read-only tools with **no model-driven execution**; cost is per-call/per-user token-capped in-memory with `estimatedCost=null` (no billing precision) — so **R3 stays OPEN until persisted budget/billing controls land**, and **R4 stays OPEN until live-output quality is evaluated beyond the deterministic guards**. No new risk row added (no new risk discovered). See `docs/execution/E47_A9_AI_RUNTIME_BOUNDARY_AND_PRODUCT_SAFETY_GATE_REPORT.md`.
- 2026-06-14 — **R4 → Mitigating (E47-A11A output safety/quality eval harness).** New deterministic output evaluator (`apps/server/src/ai/eval/output-safety/`) over a 14-category set: reuses the runtime safety+nutrition guards on output and adds output-declarative detectors (EN + Farsi) for medical/diet/vision/allergy-inference/injection-compliance/hallucinated-capability/unsafe-food/overconfident-nutrition; no LLM judge (deterministic primary gate). Offline-default (stub, zero live calls, no key) + optional live via the Orchestrator. Adversarially reviewed and **hardened** (verified evasions closed; Farsi added; over-block scoring bug fixed; adversarial-fixture proves end-to-end detection). AI suite 162/162 (+32 output-safety tests); live subset 4/4 (no false-positive on real safe output); build green; no secret/PII in artifact. **Not closed** — offline uses the stub so at-scale live-output eval + broader corpus + continuous regression remain; regex detectors have residual evasion surface. **R3 unchanged (Mitigating).** See `docs/execution/E47_A11A_LIVE_OUTPUT_SAFETY_QUALITY_EVAL_HARNESS_REPORT.md`.
- 2026-06-14 — **R4 stays Mitigating (E47-A11B regression corpus + continuous offline gate).** Expanded the A11A harness into a **54-case EN+Farsi corpus** (14 categories) + corpus schema/secret/PII validation + a CI-safe deterministic **offline gate** (`ai:eval:regression`, exits non-zero on unsafe-pass / over-block; no key/network/DB). Adversarially reviewed (3 lenses, pass_with_minor) and hardened: EN+Farsi adversarial-fixture coverage for every eval-only detector family, `sushi` food-safety vocab, validation crash-safety + phone false-positive fix + wider secret scan, natural-language prompt rephrasing, a pregnancy output-risk case, over-block + leaked-refusal + full secret-pattern tests. AI suite 176/176 (+ output-safety 46/46); build green; artifact redacted (en 33 / fa 21, 32 blocked-before-provider, 0 live calls); direct-Gemini provider-only. **Not closed** — offline-stub default; known-pattern regression only (novel-evasion fuzz/red-team is future work). **R3 unchanged (Mitigating).** See `docs/execution/E47_A11B_LARGER_REGRESSION_CORPUS_AND_CONTINUOUS_EVAL_GATE_REPORT.md`.
- 2026-06-14 — **DATA-RCP-I150 International Core 150 DB import v0.6.0** (`recipes:validate|import|verify:international-150`). Imported 150 international recipes (sequence 205–354) into the dev `garnish_db` → catalog total **350** (200 Phase-One preserved + 150 new). New `scripts/recipes/{validate,import,verify}-international-core-150-v0-6-0.js`: strict validator (counts/sequence/identity/ingredient-integrity vs the frozen **1008** dictionary + **readiness-report cross-check** — report not blindly trusted), idempotent transaction-wrapped **create-only** importer (DB identity guard; conflict-safe; reuses `mapRecipe`; corpus/source metadata in `adminNote`), and post-import verifier. **Pure create** — existing 200 recipes + all user data preserved (no update/delete/cascade); **0 new ingredient IDs**, **0 unresolved**, **0 medical/strict-diet flags**, **no DB migration**, no destructive op. Live verified: before 200 → after **350**; dry-run 0 writes; first apply created 150; second apply created 0/skipped 150 (idempotent); verifier all-PASS (350, 0 dup, 200 preserved, 0 dangling refs). Strengthens recipe-catalog coverage; adds NO new risk. Validator/planner specs **24/24**; full suite green except the 4 known **R19**; build green; QA artifact leak-free. **No AI/recommendation-ranking/notification/UI change, no product rollout. R3 & R4 remain Mitigating (NOT Closed).** See `docs/execution/INTERNATIONAL_CORE_150_DB_IMPORT_V0_6_0_REPORT.md`.
- 2026-06-15 — **E18/E43-A14 Founder results review + safe limited activation PLAN** (`recommendation:eval:activation-a14`). Reads A13 execution evidence and turns it into a Founder review + activation readiness matrix + guardrail requirements + a SAFE LIMITED ACTIVATION PLAN + blocker classification + dry-run + decision pack — WITHOUT activating anything (it plans + validates only). New `recommendation/runtime-shadow/lab/activation-review/`: config (default `MODE=off`, `REQUIRE_ADMIN=true`, `ALLOW_DRY_RUN=false`, `MAX_TRACE_READ=500`; invalid→safe; **production+dev_internal_api hard-blocked**; fail-closed; REQUIRE_ADMIN cannot weaken HTTP admin; `publicEndpointExposed` always false), A13 evidence reader (re-checks 13 safety fields — schema/runMode/failedChecks0/approvedBy-founder/no-live-or-response-change/productUse-off/failureEscape0/no-destructive-rollback/production-blocked/no-public-endpoint/promotion-not-allowed/redacted-failures-empty; missing→incomplete, never crash), Founder result review (`productionActivationAllowed`/`liveRankingActivationAllowed` always false; `founderDecisionRequired` always true), readiness matrix (11 RAG dims — **production readiness ALWAYS red, real-user readiness never green, safe-limited-dev-shadow design green when valid**), guardrail engine (**14 mandatory guardrails**), safe limited activation plan generator (**hard caps maxUsers ≤5 / maxRequests ≤240 / durationHours ≤24**, scope `dev_internal_shadow_only`; forbidden actions incl. production ranking / public personalization / AI autonomy / disable consent / disable trace redaction / bypass kill switch), blocker classifier (12 buckets; only `founder_decision_required` blocks the dev-shadow design), dry-run simulation (8 scenarios; activates nothing; live/response/product/public all literal false), decision pack (allowed `[reject, request_more_real_dev_evidence, approve_safe_limited_dev_shadow_activation]`, forbidden `[enable_production_ranking, enable_public_personalization, enable_ai_autonomy]`; founder decision required; fails closed), and a default-OFF, **admin-guarded** (`AuthGuard('jwt')`+`RolesGuard`+`@Roles('admin')`) + dry-run-gated controller returning **403** by default / in production / without allow-dry-run. **No activation, no public unauthenticated endpoint, no live ranking change, no user-visible response change, no decision trace exposed to users, no raw trace/user/recipe/AI data, no destructive retention, no migration, no data import.** Strengthens **R15/R1**; relates to **R18**. A14 gate **451/451** across 20 families (offline; access matrix 3×4×2×2; 9 evidence scenarios × full pipeline); A13 gate still **408/408**; A12 **392/392**; A11 **319/319**; full suite green except the 4 known **R19**; build green; scans clean. **R3 & R4 remain Mitigating (NOT Closed); production readiness RED; founder decision required; no UI/live-AI/notification/Food-DNA/voice/community/B2B change. A14 NOT merged.** See `docs/execution/E18_E43_A14_FOUNDER_RESULTS_REVIEW_SAFE_LIMITED_ACTIVATION_PLAN_REPORT.md`.
- 2026-06-15 — **E18/E43-A13 Founder-approved limited dev shadow experiment EXECUTION** (`recommendation:eval:experiment-a13`). The first controlled execution layer: run a bounded, Founder-approved, dev-only SHADOW recommendation experiment + capture safe redacted evidence, WITHOUT changing live ranking or the user response. New `recommendation/runtime-shadow/lab/execution/`: config (default `MODE=off`, `ALLOW_RUN=false`, `MAX_REQUESTS=120` cap 240, `TRACE_WRITE=off`, `KILL_SWITCH=off`; invalid→safe), access model (fail-closed; **production+dev_internal_api hard-blocked**; `publicEndpointExposed` always false), block-only kill switch (off mode / kill on / production / allow-run-disabled), execution manifest (Founder approval mandatory; registered A11 templates only; `requestLimit=min(requested,MAX_REQUESTS)`; production blocked; redacted trace write requires `TRACE_WRITE=redacted` + opt-in), safe in-memory run ledger (counters only, no raw data), executor (composes A11 lab runner + A9 dev-traffic batch **bounded to the request limit via an honest slice** + A8 trace analysis; optional redacted trace capture; **fails safe** on consent-bypass/response-or-ranking-mutation/raw-leak/escaped-failure), result analyzer (readiness at most `ready_for_founder_review_of_results` — never a production promotion), rollback + kill-switch **drills** (invoke the kill switch/access gate to PROVE reversibility), execution dossier (`nextDecisionRequired=founder_review_results`; forbidden next actions `[enable_production_ranking, enable_user_facing_personalization, enable_ai_autonomy]`), and a default-OFF, **admin-guarded** (`AuthGuard('jwt')`+`RolesGuard`+`@Roles('admin')`) + allow-run-gated controller returning **403** by default / in production / without allow-run. Representative shadow_dev run (honest numbers): 120 executed / 96 allowed / 24 blocked / 0 failures / 0 traces (trace-write off). **No public unauthenticated endpoint, no live ranking change, no user-visible response change, no decision trace exposed to users, no raw trace/user/recipe/AI data, no destructive retention, no migration, no data import, no autonomous agent.** Strengthens **R15/R1**; relates to **R18** (route admin-guarded + access-gated + allow-run-gated). A13 gate **408/408** across 23 families (offline; in-memory ports; access matrix 3×4×2×2; 5 templates × 2 modes); A12 gate still **392/392**; A11 gate still **319/319**; full suite green except the 4 known **R19**; build green; scans clean. **R3 & R4 remain Mitigating (NOT Closed); no UI/live-AI/notification/Food-DNA/voice/community/B2B change. A13 NOT merged.** See `docs/execution/E18_E43_A13_FOUNDER_APPROVED_LIMITED_DEV_SHADOW_EXPERIMENT_EXECUTION_REPORT.md`.
- 2026-06-15 — **E18/E43-A12 Founder-reviewed limited dev shadow experiment evidence pack** (`recommendation:eval:founder-review-a12`). INTERNAL/DEV-only evidence system that makes the A11 lab usable for a real Founder review decision. New `recommendation/runtime-shadow/lab/founder-review/`: config (default `MODE=off`, `REQUIRE_ADMIN=true`, `MAX_TRACE_READ=500`, `ALLOW_RUN=false`; invalid→safe), access model (fail-closed; **production+dev_internal_api hard-blocked**; `publicEndpointExposed` always false), Founder-review plan model (scope `dev_shadow_only`; `requiredApprovals` always `[founder]`; `promotionAllowed`/`liveRankingChangeAllowed`/`userResponseChangeAllowed` always false; unknown template → blocked), evidence pack generator (integrates A11 lab summary + A10 control-plane summary + A9 simulation artifact + A8 redacted trace analysis + A8 retention dry-run; missing input → incomplete with blocker, never a crash), safety checklist engine (any critical failure → blocked; redaction/leak scan), rollback/kill-switch **proof** engine (invokes the A11 kill switch to PROVE it blocks on every unsafe condition; env-only disable; no destructive rollback), dossier generator (verdict + allowed options `[reject, request_more_evidence, approve_limited_dev_shadow_experiment]`, forbidden `[enable_production_ranking, enable_user_facing_personalization, enable_ai_autonomy]`; Founder decision always required; next gate **A13**), Founder decision placeholder, and a default-OFF, **admin-guarded** (`AuthGuard('jwt')`+`RolesGuard`+`@Roles('admin')`) + access-gated controller/service returning **403** by default / in production. **No public unauthenticated endpoint, no live ranking change, no user-visible response change, no decision trace exposed to users, no raw trace/user/recipe/AI data, no destructive retention, no migration, no data import, no autonomous agent.** Strengthens **R15/R1**; relates to **R18** (route admin-guarded + access-gated). A12 gate **392/392** across 22 families (offline; in-memory ports; access matrix 3×4×2×2; 5 templates × 2 allowRun modes); A11 gate still **319/319**; full suite green except the 4 known **R19**; build green; scans clean. **R3 & R4 remain Mitigating (NOT Closed); no UI/live-AI/notification/Food-DNA/voice/community/B2B change. A12 NOT merged.** See `docs/execution/E18_E43_A12_FOUNDER_REVIEWED_LIMITED_DEV_SHADOW_EXPERIMENT_EVIDENCE_PACK_REPORT.md`.
- 2026-06-15 — **E18/E43-A11 Recommendation Lab / controlled dev experiment engine** (`recommendation:eval:lab-a11`). INTERNAL/DEV-only controlled-experiment engine: run bounded, named shadow recommendation experiments → quality scorecard + promotion-readiness decision, WITHOUT changing live ranking or the user response. New `recommendation/runtime-shadow/lab/`: config (default `MODE=off`, `ALLOW_DRY_RUN=false`, `MAX_REQUESTS=240`, `MAX_TRACE_READ=500`, `KILL_SWITCH=off`; invalid→safe), access model (fail-closed; **production+dev_internal_api hard-blocked**; off→blocked; `publicEndpointExposed` always false), config validator (blocks production/over-bound/unknown-context/no-consent-trace-write/unsafe-profile/kill-switch), **5-template registry** (no arbitrary code), experiment runner (composes A9 dev-traffic batch + A8 read-only online analysis + A8 retention dry-run; offline/deterministic/never-throws), quality scorecard (safety violations dominate → 0; garbage→0 fail-closed), **safety kill switch** (block-only; cannot be bypassed; invalid context→blocked), promotion gate (`allowed` **always false**, next gate **A12**, never closes R3/R4), report generator (no raw trace/PII), and a default-OFF, **admin-guarded** (`AuthGuard('jwt')`+`RolesGuard`+`@Roles('admin')`) + access-gated controller (`GET/POST /internal/recommendation-shadow/lab/{summary,run}` → **403** by default / in production; `run` accepts only a registered key). **No public unauthenticated endpoint, no live ranking change, no user-visible response change, no decision trace exposed to users, no raw trace/user/recipe/AI data, no destructive retention, no migration, no data import, no autonomous agent.** Strengthens **R15/R1**; relates to **R18** (route admin-guarded + access-gated). A11 gate **319/319** across 18 families (offline; in-memory ports; access matrix 3×4×2×2; 5 templates × 2 modes; scorecard/promotion tamper cases); A10 gate still **234/234**; full suite green except the 4 known **R19**; build green; scans clean. **R3 & R4 remain Mitigating (NOT Closed); no UI/live-AI/notification/Food-DNA/voice/community/B2B change. A11 NOT merged.** See `docs/execution/E18_E43_A11_RECOMMENDATION_LAB_CONTROLLED_DEV_EXPERIMENT_ENGINE_REPORT.md`.
- 2026-06-14 — **E18/E43-A10 Limited dev experiment control plane + internal metrics** (`recommendation:eval:shadow-a10`). INTERNAL/DEV-only, read-only control plane to inspect shadow recommendation quality + experiment readiness. New `recommendation/runtime-shadow/control-plane/`: config (default `MODE=off`, `REQUIRE_ADMIN=true`, `MAX_TRACE_READ=500`; invalid→safe), access model (fail-closed; **production+dev_internal_api hard-blocked**; off→blocked; `publicEndpointExposed` always false), summary service (composes A8 online-analysis over redacted traces + A8 retention dry-run + A9 simulation quality/buckets/performance + a safe non-blind A9-artifact reader), promotion gate (`allowed` **always false**, next gate A11, never closes R3/R4), and a default-OFF, **admin-guarded** (`AuthGuard('jwt')`+`RolesGuard`+`@Roles('admin')`) + access-gated controller returning **403** by default / in production. **No public unauthenticated endpoint, no live ranking change, no user-visible response change, no decision trace exposed to users, no raw trace/user/recipe data, no destructive retention, no migration.** Strengthens **R15/R1** + gives internal experiment-readiness visibility; relates to **R18** (route is admin-guarded + access-gated, not root-mounted). A10 gate **234/234** across 19 families (offline; in-memory ports); A9 gate still green; full suite green except the 4 known **R19**; build green; scans clean. **R3 & R4 remain Mitigating (NOT Closed); no UI/live-AI/notification/Food-DNA/voice/community/B2B change.** See `docs/execution/E18_E43_A10_LIMITED_DEV_EXPERIMENT_CONTROL_PLANE_INTERNAL_METRICS_API_REPORT.md`.
- 2026-06-14 — **E18/E43-A9 Controlled dev-traffic shadow experiment simulation + quality gate** (`recommendation:eval:shadow-a9`). Offline, deterministic simulation driving the A8 shadow service (in-memory ports) across **24 synthetic users × 7 contexts × 6 consent states** (≥240 requests, ≥80 allowed / ≥40 blocked / ≥20 trace-eligible / ≥10 retention-eligible) + quality thresholds + promotion-readiness decision + 12 failure buckets + performance guard + A8 online-analysis/retention integration. New `recommendation/runtime-shadow/`: dev-traffic types + simulator, quality-thresholds (`evaluateShadowRecommendationQuality` — meaningful safety thresholds; **`promotionAllowed` always false**), failure-buckets (counts only, no raw examples), performance-guard, and a **404-check** A9 QA gate. Personas are purely behavioral (NO protected/medical attributes). **No new migration, no network, no real DB IO, no live ranking change, no user-visible response change, no decision trace exposed to users, no destructive retention.** Strengthens **R15/R1** (consent-gated, redacted simulation; readiness evidence). A9 gate **404/404** across 16 families; A8 gate still green (45/45); full suite green except the 4 known **R19**; build green; scans clean. Best outcome = `ready_for_limited_dev_experiment_design` (next gate **A10**, Founder-approved/consent-gated, still no live ranking change). **R3 & R4 remain Mitigating (NOT Closed); no UI/live-AI/notification/Food-DNA/voice/community/B2B change.** See `docs/execution/E18_E43_A9_CONTROLLED_DEV_TRAFFIC_SHADOW_EXPERIMENT_SIMULATION_QUALITY_GATE_REPORT.md`.
- 2026-06-14 — **E18/E43-A8 Persisted signal/profile feed + consent plumbing + online shadow analysis** (`recommendation:eval:shadow-a8`). Closes A7's biggest gaps while staying default-OFF and product-disabled. New `recommendation/runtime-shadow/`: persisted profile feed (rebuild a UserFoodIdentityGraph from persisted `SignalObservation` rows via the A4 builder, or persisted snapshot, else cold-start — never fabricated), consent plumbing over `ConsentLog` (request-preferred, user-settings fallback, dev-fixture isolated, fail-closed; purposes behavioralAnalytics/personalizationShadow/recommendationExperiment/traceStorage → canonical analytics/personalization), read-only online analysis over redacted traces, dry-run retention readiness (NEVER deletes; requiresFounderApproval), a consent-aware `RecommendationShadowA8Service` (now the pipeline's shadow hook; A7 service untouched), and A8 metrics. **No new DB migration** (reuses the A7 `RecommendationShadowTrace` table + existing `SignalObservation`/`ConsentLog`/`UserBehaviorProfile`). **No live ranking change, no user-visible response change, no decision trace exposed to users, no destructive retention, no DB IO in default-off mode.** Strengthens **R15/R1/R16** (consent-gated, redacted, retention-aware). A8 gate **225/225** across 18 families (offline, in-memory ports); A7 gate still green; runtime-shadow suites **169/169** (A6 integration spec green under the re-pointed A8 hook); full suite green except the 4 known **R19**; build green; scans clean. **R3 & R4 remain Mitigating (NOT Closed); no UI/live-AI/notification/Food-DNA/voice/community/B2B change.** See `docs/execution/E18_E43_A8_PERSISTED_SIGNAL_PROFILE_FEED_CONSENT_PLUMBING_ONLINE_SHADOW_ANALYSIS_REPORT.md`.
- 2026-06-14 — **E18/E43-A7 Scoring-input provider + redacted trace persistence + experiment readiness** (`recommendation:eval:shadow-a7`). Makes the A6 shadow system operationally useful while staying default-OFF and product-disabled. New `recommendation/runtime-shadow/`: safe scoring-input provider (`buildRecommendationShadowInputs` — consent hard-gate, no fabrication, cold-start fallback graph), bounded read-only Prisma data port (safe projection — never body/steps), trace redaction + cleanliness guard, trace store (Noop default + Prisma adapter, writes ONLY redacted + ONLY when `RECOMMENDATION_SHADOW_TRACE_WRITE_MODE=redacted`), deterministic default-OFF experiment (`RECOMMENDATION_SHADOW_EXPERIMENT_MODE`), and pure metrics. **Additive** Prisma model `RecommendationShadowTrace` (migration `20260614020000` — CREATE TABLE + 3 indexes, no FK/cascade/backfill, non-destructive); default writes OFF. Service `observe` extended (A6 provider path preserved); pipeline hook unchanged. **No live ranking change, no user-visible response change, no decision trace exposed to users, no product personalization, no DB read/write in default-off mode.** Strengthens **R15/R1** (consent-gated, redacted, leak-free). A7 gate **210/210** across 15 families (offline, Prisma mocked); A6 gate still green (124/124 incl. preserved A6 service spec); full suite green except the 4 known **R19**; build green; static scans clean (forbidden terms + synthetic fixtures only in denylist/tests). **R3 & R4 remain Mitigating (NOT Closed); no UI/live-AI/notification/Food-DNA/voice/community/B2B change.** See `docs/execution/E18_E43_A7_SCORING_INPUT_PROVIDER_TRACE_PERSISTENCE_AND_EXPERIMENT_READINESS_REPORT.md`.
- 2026-06-14 — **E18/E43-A6 Shadow Runtime Recommendation Integration Gate v1** (`recommendation:eval:shadow-runtime`). Wires the A5 SHADOW decision engine to run **beside** the live recommendation pipeline behind a default-OFF env gate (`RECOMMENDATION_SHADOW_RUNTIME_MODE=off|shadow`, `..._SAMPLE_RATE=0`). New `recommendation/runtime-shadow/`: pure runtime gate (deterministic FNV sampling + fail-closed safety gate + A5 scorer call + comparison + divergence + redaction; never throws/mutates), a live-vs-shadow comparison, a 13-code divergence analyzer, and a thin `@Optional()` NestJS hook called in `RecommendationPipelineService.getRecommendations` **after** live ranking with a fresh copy of recipeIds (result discarded). **No live ranking change, no user-visible response change, no DB write, no migration, no network** — proven by an integration spec (response deep-equal shadow-OFF vs ON; throwing shadow can't break the request; no shadow keys in the response). Live scoring-input provider intentionally **not wired** (default path → 0 DB reads; full graph/candidate/history feed is A7). **280-check** offline QA gate (12 users / 30 candidates / 8 histories → 12 distinct shadow fingerprints; `rankingChangedForUser`/`productUseEnabled`/`dbMigrationRequired` all false; artifact leak-free). Strengthens **R15/R1**; adds NO new risk. A1 78 · A2 63 · A3 54 · A4 49 · A5 34 · A6 78 specs/280-check gate; full suite green except the 4 known R19; build green. **No persistence, no product personalization, no notification/Food-DNA/AI/voice enablement, no live AI. BIP v1 NOT complete. R3 & R4 remain Mitigating (NOT Closed); no UI/community/B2B change.** See `docs/execution/E18_E43_A6_SHADOW_RUNTIME_RECOMMENDATION_INTEGRATION_GATE_REPORT.md`.
- 2026-06-14 — **E18/E43-A5 Recommendation Decision Intelligence v1 (shadow) + Exposure/Outcome Attribution + Why Engine** (`recommendation:eval:decision-intelligence`). SHADOW decision brain under `recommendation/intelligence/`: UserFoodIdentityGraph + candidates + exposure/outcome history → `RecommendationDecisionTrace` (transparent weighted scoring — taste/effort/skill/routine/feedback/novelty fits − fatigue/safety/confidence penalties; documented weights, NO black-box ML), pure exposure + outcome attribution, and a safe Why Engine (reason codes + anti-manipulative/creepy/medical scanner reusing the A3 forbidden-term guard). **Additive + pure** — no DB writes, **no migration**, **no live ranking change**, no wiring into the existing recommendation pipeline. 12-user × 30-candidate × 8-history simulation → **12 distinct ranking fingerprints, 0 collapsed, 12 context-shift cases**. **209-check** offline QA gate (0 DB/live; adversarially reviewed — folded in a weekday-effort-fit fix so time-availability dominates attitude); `productUseEnabled` + readiness `safeForProductUse` always false; artifact leak-free, no medical/protected label. Strengthens **R15/R1**; adds NO new risk. A1 78 · A2 63 · A3 54 · A4 49 · A5 34 specs/209-check gate; full suite green except the 4 known R19; build green. **No product personalization, no notification/Food-DNA/AI/voice enablement, no prediction, no migration. BIP v1 NOT complete. R3 & R4 remain Mitigating (NOT Closed); no UI/live-AI/community/B2B change.** See `docs/execution/E18_E43_A5_RECOMMENDATION_DECISION_INTELLIGENCE_EXPOSURE_OUTCOME_ATTRIBUTION_WHY_ENGINE_REPORT.md`.
- 2026-06-14 — **E43-A4 UserFoodIdentityGraph v1 + Behavioral Profile Snapshot Readiness** (`behavior:eval:profile`). Next intelligence layer: SignalObservations → user-level multi-dimensional graph (11 dimensions) → downstream readiness contracts. **Pure** (`behavior-engine/profile/`, no DB writes; **no migration** — snapshot models already exist), additive to the legacy `identity/` builders. Transparent confidence aggregation (recency-weighted, evidence-weighted overall), conflict resolution (never erases evidence; context-splits vs contradictions; lowers confidence), evidence lineage, freshness/recency, **graph-level privacy** (highest-class-wins, P2 never downgraded, b2b blocked, `containsMedicalOrProtectedInference` always false, anti-laundering graph scanner), safe explanations only, and six downstream readiness contracts (`safeForProductUse` ALWAYS false). A **12-persona** simulation proves distinct profiles (**12 distinct fingerprints, 0 collapsed**). **157-check** offline QA gate (0 DB/network); artifact leak-free, no medical/protected label. Strengthens **R15/R1** (PII-free, consent-gated, leak-free behavioral data); adds NO new risk. A1 gate 78 · A2 63 · A3 103 · A4 157; full suite green except the 4 known R19; build green. **No persistence, no runtime wiring, no prediction, no recommendation-ranking/notification/Food-DNA/AI-personalization/voice change, no product enablement. BIP v1 NOT complete. R3 & R4 remain Mitigating (NOT Closed); no UI/live-AI/community/B2B change.** See `docs/execution/E43_A4_USER_FOOD_IDENTITY_GRAPH_AND_BEHAVIORAL_PROFILE_SNAPSHOT_READINESS_REPORT.md`.
- 2026-06-14 — **E43-A3 SignalObservation Engine + Signal Registry v1** (`behavior:eval:signals`). First behavioral-interpretation layer: events → typed signal observations. Added a typed, **forbidden-signal-guarded** registry (44 signals / 10 families, each with confidence policy + privacy/consent/retention + forbidden inferences + safe explainability template), a **pure deterministic** extraction engine (`extractSignalObservations` — no DB/network/throw; consent-gated; never downgrades P2; deterministic order), a transparent confidence model (recency half-life decay, contradiction-aware, cap 0.95 without explicit-feedback+repeated evidence), explainability guards (no creepy/medical/identity phrasing), and a **103-check** offline QA gate. **No DB migration** (all signal/snapshot Prisma models already exist; engine is pure). Strengthens **R15/R1** (PII-free, consent-gated, leak-free behavioral data) and adds NO new risk. **Optional hardening:** added a circular-ref/depth guard to `redactEventEnvelopeForArtifact` (A1/A2 follow-up) + regression tests. A1 gate 78 · A2 gate 63 · A3 gate 103/103 (0 DB/live); full suite green except the 4 known R19; build green. **No persistence, no runtime wiring, no recommendation/notification/AI-personalization change, no UserFoodIdentityGraph, no medical/diagnostic/protected-attribute inference. BIP v1 NOT complete; analytics NOT fully migrated. R3 & R4 remain Mitigating (NOT Closed); no product/UI/live-AI/community/B2B change.** See `docs/execution/E43_A3_SIGNAL_OBSERVATION_ENGINE_AND_SIGNAL_REGISTRY_V1_REPORT.md`.
- 2026-06-14 — **E43-A2 taxonomy-bound producer migration foundation + shadow runtime integration** (`analytics:eval:event-producers`). Bound the envelope contract to the legacy `EventType` taxonomy (`event-taxonomy.contract.ts`: 11 families + per-family defaults; `validateEventType` strict-rejects/shadow-warns unknown types; planned canonical types marked `canonical_planned`, never reported as legacy). Added a **code-grounded** producer inventory (`event-producer-inventory.ts`: 20 producers via `targeted_static_scan`; honest family gaps for cook/admin/workflow/notification-suppression) + a migration map (`docs/analytics/E43_A2_EVENT_PRODUCER_MIGRATION_MAP.md`). Added a **pure** runtime guard (`event-envelope-runtime-guard.ts`: off/shadow/strict; no DB/network/throw; always returns a redacted copy) and wired it into **one** low-risk path (`analytics.service.trackEvent`) in **observational shadow** mode (default-safe; never drops/alters events, no new DB write, redacted debug logs only; env `EVENT_ENVELOPE_RUNTIME_GUARD_MODE`). Strengthens **R15** (taxonomy/consent discipline at ingest) and **R1** (no secret/PII in event logs/artifacts). Producer gate **21/21** (0 DB/live); E43-A1 gate still green; full suite 422/426 (only the 4 known R19); build green. Adversarially reviewed (4 lenses) — folded in a taxonomy fix (4 canonical-planned social/b2b types now classify to real families, not 'unknown'). **No DB migration; no producer emits canonical_v2 yet (staged). BIP v1 NOT complete; analytics NOT fully migrated. R3 & R4 remain Mitigating (NOT Closed); no product/UI/live-AI/community/B2B change.** See `docs/execution/E43_A2_TAXONOMY_BOUND_EVENT_PRODUCER_MIGRATION_AND_SHADOW_RUNTIME_INTEGRATION_REPORT.md`.
- 2026-06-14 — **E43-A1 Canonical Event Envelope code contract + ingest gate** (`analytics:eval:event-envelope`). Hardened the schemaVersion-2 envelope contract into a producer-ready boundary: added `EventSourceEnum`, the canonical `{ ok, value, errors, warnings }` validator result, `normalizeLegacyEventEnvelope` (backward-tolerant; **never fabricates consent** — legacy w/o consent + no caller default → rejected; untrusted legacy `payload` dropped, not copied), `redactEventEnvelopeForArtifact`, and **broadened deterministic PII/secret value detectors** (JWT / Bearer / API-key / private-key / DB-conn-string / embedded Iranian phone / long free-text) — strengthens **R15** (consent/PII-at-ingest) and **R1** (no secret/PII in event metadata/artifacts), and gives **R16** a typed `subjectId` erasure key. New `event-envelope.ingest-gate.ts` (pure: **no DB write, no network, no throw**) + 15 canonical examples + a deterministic offline contract gate (**46/46 checks**, 0 live/DB) writing `docs/qa/analytics/e43_a1_event_envelope_contract_results.json`. **No DB migration** (code contract only); gate **not wired into the live ingest path** (Founder-gated log-only adoption stays deferred per E43-W6). Full server suite 359/363 (only the 4 known R19 specs fail); build green. Adversarially reviewed (4 lenses) — folded in a phone-detector precision fix (bare 8–15 digit ids no longer false-flagged as phones; Iranian/formatted phones still caught). **R3 and R4 remain Mitigating (NOT Closed); no product/UI/live-AI change, no recipe/ingredient change, no destructive retention.** See `docs/execution/E43_A1_CANONICAL_EVENT_ENVELOPE_CODE_CONTRACT_AND_INGEST_GATE_REPORT.md`.
- 2026-06-14 — **E47-A12 AI internal pilot-readiness failure-injection gate** (`ai:eval:pilot-readiness`). Deterministic, CI-safe (no key/network/DB; mock Prisma + fixture providers) gate of **34 checks** across flags / orchestrator-only / guard-order (fail-closed + best-effort + cost-before-safety precedence) / A11B-integration / failure-injection / secret-hygiene / docs-honesty; adversarially reviewed (3 lenses) and hardened (genuine spend-alert failure injection, real A11B-script assertion, counters documented as invariants-by-construction). Diagnostic-route protection evidenced (RolesGuard admin-only / deny-by-default; diagnostics controller class-guarded `AuthGuard('jwt')+RolesGuard+@Roles('admin')` — root-mount remains **R18** design debt). **Secret-hygiene:** scrubbed a pre-existing hardcoded dev JWT + embedded phone from `scripts/dev/test-exposure-final.js` (now env-var-gated; repo secret scan clean) and the gate now enforces no hardcoded secret/PII in `scripts/dev/` going forward (relates to **R1**). AI suite 187/187; build green; no runtime/schema/UI change. **R3 and R4 remain Mitigating (NOT Closed); no product rollout.** See `docs/execution/E47_A12_AI_INTERNAL_PILOT_READINESS_FAILURE_INJECTION_GATE_REPORT.md`.
