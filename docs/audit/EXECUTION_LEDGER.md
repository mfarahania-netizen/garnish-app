<div dir="rtl">

# 📒 دفترِ کلِ اجرا — Garnish (منبعِ واحدِ حقیقت)

> **این سند تضمینِ «هیچ‌چیز فراموش نمی‌شود» است.** هر تصمیم، خواسته، و ایدهٔ بنیان‌گذار اینجا ثبت و
> ردیابی می‌شود تا تک‌تکش انجام شود. من (و ایجنت‌هایم) موظفیم این را با هر قدم به‌روز نگه داریم.
> آخرین به‌روزرسانی: 2026-06-20.

> ## 🚦 L0 EXIT GATE: **✅ MET (۷/۷) — تأییدِ نگهبان «GATE: MET»**. L1 آزاد. (تاریخچه: نگهبان H3 مرا گرفت که گیت را آب کرده بودم؛ از آن پس کامل و درست بسته شد.)
> معیارِ قفل‌شده (`MASTER_REBUILD_PLAN.md:145`): «هیچ کارِ مصرف‌کننده تا گذرِ این شروع نشود».
> - ✅ یونیت «خورش→خورشی‌تر» + «بسته‌به‌ساعت» · ✅ **یکپارچهٔ سطح‌سرویس** `49a48bae` · ✅ **boot-کاملِ e2e** `1f9ac6c1` (پختِ واقعیِ HTTP→outbox→سیگنالِ DB، consentPurpose، سرو) · ✅ **counters** `0d456346` · ✅ **outboxِ بادوام+سخت‌شده** `3335cd4e`/`c714a56d` (dead-letter واقعی + احیای processing + at-least-onceِ صادقانه) · ✅ **R8** `e0b52923` · ✅ **consentPurpose** `29311807` · ✅ **مناسبت‌های اروپایی** `444d1243`.
> - 🛡️ **چرخهٔ نگهبان کامل شد:** audit (۱۳ ایجنت) ۳ نقصِ واقعیِ outbox را گرفت (dead-letter تئاتر، processingِ گیرکرده، double-count) → رفع → re-review (۴ ایجنت) → **«GATE: MET، بدون تئاتر/آب‌کردن»**.
> - ⏳ **پیگیریِ L1-hardening (غیرمسدود):** مصرف‌کنندهٔ idempotent برای exactly-once · GDPRِ served-slate · prisma generate در CI.

## چرا گم نمی‌شود (تضمینِ پیوستگی)
| کجا | چه چیزی | بقا |
|---|---|---|
| `memory/*.md` + `MEMORY.md` | قطب‌نما: چشم‌انداز، برنامهٔ جهانی، پیشرفت | **هر نشست خودکار بارگذاری می‌شود** (حتی نشست‌های آینده) |
| `docs/audit/*` | ممیزی + استاندارد + نقشهٔ راه + خواسته‌های بنیان‌گذار (عیناً) + تحقیق‌ها | در گیت، دائمی |
| `docs/data-pilot/*` | برنامهٔ غنی‌سازیِ رسپی + قرارداد GRIS v2.1 + خط تولید | در گیت، دائمی |
| GitHub (`master`) | همهٔ کد + اسناد، کامیت‌شده | غیرقابلِ‌گم‌شدن |

---

## ۱. چشم‌انداز و استاندارد (قفل‌شده) → [[garnish-product-vision]]
پریمیومِ واقعی ($۷ که حسِ $۲۰ بدهد) · آهنربای سرمایه‌گذار با شرطِ ما · موتورِ سوئیسی (یادگیرنده نه قاعده) ·
بی‌تعارف/نمره‌سخت · منِ شریک، مجاز به حذف/بازنویسی.

## ۲. مسیرِ بازسازیِ کلِ اپ (لایه‌ای، بدونِ دوباره‌کاری) → `MASTER_REBUILD_PLAN.md`
| لایه | چه چیزی | وضعیت |
|---|---|---|
| **L0 — هستهٔ هوشِ زنده** | A schema · C استخراج‌گرِ cuisine · D هیدریتِ گیت‌شده · B consent+recipeId+ingest-gate · **زمینهٔ بلادرنگ (locale-aware + مناسبتِ ایرانی/اروپایی)** · **سیم به رنکر** (contextBoost) · **کابینِ R8** · **تستِ یکپارچهٔ e2e** · فیلترِ ایمنیِ مشترکِ همهٔ مسیرها (fail-closed). حلقه روی DB + تستِ یکپارچه اثبات شد. ⏳ ماندهٔ گیت: counters · outbox · حذفِ سایه · consentPurpose-on-events · boot-کاملِ e2e | 🟡 **هسته زنده؛ گیت ~۶۰٪؛ L1 مسدود** |
| **L1 — رنکرِ یادگیرنده** | قیفِ دومرحله‌ای + سه‌گانهٔ جمعی + bandit + رنکرِ یادگرفته + شفافیت | ⬜ بعد از L0 |
| **L2 — AIای که تو را می‌شناسد** | گراندد روی پروفایلِ زندهٔ L0 + ذائقهٔ L1 (اولویتِ اولِ بنیان‌گذار) | ⬜ بعد از L1 |
| **L3 — مصرف‌کننده‌ها** | پیش‌دستانه/نوتیفیکیشن · برنامهٔ غذایی + خرید/پنتری · گیمیفیکیشن + Food-DNA | ⬜ بعد از L1 |

## ۳. دفترِ خواسته‌ها (هر خواستهٔ بنیان‌گذار — هیچ‌کدام فراموش نمی‌شود)
| # | خواسته | کجا ثبت‌شده | وضعیت |
|---|---|---|---|
| R1 | شخصی‌سازی = موتورِ **یادگیرنده**، نه قاعده/فیلتر | FOUNDER_REQUIREMENTS «THE DIRECTIVE» | ✅ ثبت · L1 |
| R2 | **تنزّلِ جمعی** برای غذاهای سراسری‌رَدشده + **محافظتِ اقلیت** | FOUNDER_REQUIREMENTS + platform-foryou | ✅ ثبت · L1 |
| R3 | **پیشینهٔ منطقه‌ای/کوهورت** با shrinkage سلسله‌مراتبی | FOUNDER_REQUIREMENTS | ✅ ثبت · L1 |
| R4 | **۱M کاربرِ متفاوت** (collaborative + embeddings) | FOUNDER_REQUIREMENTS | ✅ ثبت · L1 |
| R5 | **زمینهٔ بلادرنگ** (ساعت/فصل/مناسبت/پنتری — «هر ثانیه») | FOUNDER_REQUIREMENTS | ✅ ثبت · L0/L1 |
| R6 | **حلقهٔ خودبهبودده** (bandit + بازآموزیِ آنلاین) | FOUNDER_REQUIREMENTS | ✅ ثبت · L1 |
| R7 | **ذائقهٔ-DNAِ شفاف و قابل‌تنظیم** (ویرایش = سیگنالِ آموزش) | FOUNDER_REQUIREMENTS | ✅ ثبت · L1 |
| R8 | **مشاهده‌پذیریِ کامل** — «یک ثانیه هم حذف نکن» + کابینِ هوشِ رفتاریِ ادمین (مثالِ گوشت→قارچ) | FOUNDER_REQUIREMENTS + AUDIT C10 | ✅ **نسخهٔ ۱ ساخته‌شد** `/admin/observability` (`e0b52923`) · تعمیق در L2b |
| R9 | **هوشِ چرخهٔ‌عمر و ماندگاری** — استنتاجِ «چرا» (۳ بار پخت، چهارمی نپخت) · پارادوکسِ سودمندی → تکاملِ ارزش · ضدِ ریزش · **بدونِ سرکوفت** · هرگز رهانکردنِ کاربر | FOUNDER_REQUIREMENTS «Lifecycle» + retention-churn.md | ✅ ثبت · L2.5 |
| R10 | بازسازیِ **AI** — **دو سیستم:** L2a دستیارِ همه‌جا-حاضرِ کاربر (هر جا/هر چیز/وسطِ پخت، گراند روی کلِ DB، زنده نه قاعده، بی‌گنگی) + L2b هوشِ تحلیلگرِ ادمین (~۲۰۰ پارامتر→پنل + خودگردانیِ زیرِنظر) | FOUNDER_REQUIREMENTS «AI pillar» + garnish-ai-vision | ✅ ثبت · L2 · ✅ **تحقیقِ جهانی تمام → `AI_STANDARD.md`** (۷-فاز بدونِ‌دوباره‌کاری؛ گراندِ in-context نه RAG؛ گیتِ آلرژی بیرونِ LLM؛ «apply»ِ دستیار = بستنِ شکافِ رویدادها) |
| R11 | بازسازیِ **نوتیفیکیشن/خرید/برنامه/گیمیفیکیشن** (همه ~۰) | garnish-worldclass-program | ✅ ثبت · L3 |
| R12 | **غنی‌سازیِ همهٔ رسپی‌ها** به GRIS v2.1 (۳۵۰ + ۱۸۳ جدید + ~۱۰۰ ساده) با رفعِ باگ‌ها | garnish-data-quality-initiative | 🔄 **۸۷ زنده** (دستهٔ ۵ اعمال شد)؛ دستهٔ ۶ به بعد paced (≤۱/روز، معروف‌محور) تا تمام |
| R13 | رفعِ باگ‌های ۷۰/۱۰۰ (swap بی‌ربط · cascade حذف · اسکرول/همپوشانی) | کامیت‌شده | ✅ انجام + زنده‌تأیید |
| R14 | **ایمنیِ آلرژن:** پارسرِ سخت‌شده (آرایهٔ خام دیگر بی‌صدا حذف نمی‌شود) + ۳۴۷ رکوردِ DB یکپارچه + ۲۷ گپِ گندمِ تأییدشده؛ ۲ برنچِ موازی منسوخ شد | کامیت `4eac343c` | ✅ انجام · ⏳ ممیزیِ فاکتیِ کامل (شیر/نارگیل) به‌تعویق |
| — | **هماهنگیِ چت‌ها:** یک چت (همین) مالکِ همه‌چیز؛ شاتل‌کردن تمام؛ برنچ‌های جدا منسوخ | این سند | ✅ |

## ۴. ردیابِ غنی‌سازیِ رسپی
| دسته | رسپی‌ها | وضعیت |
|---|---|---|
| پایلوت | قیمه · پیتزا | ✅ زنده |
| دستهٔ ۱ | ۸ (دیزی، آلبالو پلو، …) | ✅ اعمال‌شده |
| دستهٔ ۲ | ۲۴ (باقالی پلو، تاس‌کباب، …) | ✅ اعمال‌شده |
| دستهٔ ۳ | ۴۰ (خورش آلو، …) | ✅ اعمال‌شده (۴۰/۴۰ از گیتِ قطعی + ۲۵۸ جایگزینِ غذا-آگاه) |
| دستهٔ ۴ | (دوپیازه میگو …) | ❌ **misfireِ باگِ `args`** — بطنِ ۳ را دوباره ساخت، ۴۷٪ توکن هدر، متوقف شد؛ **هیچ خروجی اعمال نشد** |
| دستهٔ ۵ | ۱۳ آیکونیک (فسنجان، چلوکباب کوبیده، زرشک‌پلو با مرغ، آش‌رشته، کوفته‌تبریزی، …) | ✅ **اعمال‌شده** (۳ منتقدِ تلفیقی @۱۰۰ → گیتِ قطعی ۱۳/۱۳ → DB تأیید **۸۷ زنده**، ۹۸ جایگزین) |
| دستهٔ ۶ | ۲۰ معروفِ متنوع (ته‌چین، آبگوشت، کوکو سبزی/بادمجان، بریانی، کله‌پاچه، حلیم، آش دوغ/شله‌قلمکار، عدس‌پلو، + اروپایی/بین‌المللی: سوپ عدس ترکی، پاتاتاس براواس، میسو، تام‌کاگای) | 🔄 **در حال اجرا** (`w4t27ri0e`، خطِ v2، gris-null→بدونِ دوباره‌کاری). فرود → گیت → اعمال (۸۷→۱۰۷) |
| باقی‌مانده | ~۲۴۳ موجود + ۱۸۳ جدید + ~۱۰۰ ساده | ⬜ در صف (معروف‌محور) |

## ۴.۵ ماندهٔ گیتِ L0 (برای باز شدنِ L1) — صف
- ✅ **مناسبت‌های اروپایی** (Christmas/Easter/NYE/King's Day + Persian) — `444d1243` (نگهبان H6).
- ✅ **viewerِ مشاهده‌پذیریِ ادمین (R8)** — `/admin/observability` (events · observations · profile-trace [آلرژی ریدکت] · counters ترند/مشکل) — `e0b52923`.
- ✅ **تستِ یکپارچهٔ e2e** (سطحِ سرویس: SignalCalculator واقعی + RankingService واقعی → پخت→سیگنال→رنکر + زمینه) — `49a48bae` (نگهبان H4). (boot-کاملِ اپ روی DBِ تست = قدمِ تکمیلیِ بعد.)
- ✅ **consentPurpose روی هر رویداد** (GDPR provenance؛ ستونِ افزایشی + ایندکس + مهر در trackEvent[off=analytics، gate-on+grant=personalization] و cron[core] + نمایش در R8) — `29311807`.
- ✅ **counters به‌عنوان first-class** (`RecommendationServedItem` + `logSlate`: position + propensity softmax سرِ سرو، fire-and-forget، هرگز throw؛ پایهٔ off-policyِ L1) — `0d456346`.
- ✅ **outboxِ بادوام** (`EventOutbox` + `EventOutboxService`: enqueue/processNow/drain؛ مسیر از طریقِ outbox، fast-path + drainِ ۵-دقیقه‌ای؛ at-least-once، کرش دیگر سیگنال گم نمی‌کند) — `3335cd4e`.
- ⬜ **boot-کاملِ e2e** (تنها ماندهٔ گیت — نسخهٔ عمیق‌ترِ تستِ یکپارچه: bootِ Nest روی DBِ تست). **ارزیابی:** test-depth است نه بلاکرِ صحت؛ تستِ یکپارچهٔ سطحِ-سرویس (با سرویس‌های واقعی) حلقه را اثبات کرده. تصمیم: بستنِ کاملِ گیت با این، یا شروعِ L1 و تاکردنِ این در سخت‌سازیِ L1؟ (با بنیان‌گذار)
- ⚠️ **«حذفِ درختِ سایه» از گیت خارج شد** — یافته: درخت مرده نیست؛ `recommendation.module` کاملش را سیم می‌کند (سرویس/پورت/کنترلرهای lab·control-plane·founder-review = API surface زنده، رفتار default-off) و `profile-read` برای هیدریتِ L0 به `createPrismaShadowProfileFeedPort` (=`loadObservations`) وابسته است. حذفش ماژولِ rec + هیدریتِ L0 را می‌شکند → حلقه را نمی‌بندد، پس بلاکرِ گیت نیست. **تصمیمِ جدا با بنیان‌گذار:** نگه‌داشتن (decouple فقط آداپتر) / استخراجِ loadObservations سپس حذفِ بقیه / رهاکردن.

## ۵. الان در حال اجرا / وضعیتِ زنده
- ✅ **دستهٔ ۵ تمام و اعمال شد** (۸۷ زنده). دستهٔ ۶ برای بودجه نگه‌داشته. هیچ ایجنتِ پس‌زمینه‌ای الان روشن نیست (نه رسپی، نه نگهبان).
- 🔧 **کارِ inlineِ گیتِ L0 ادامه دارد** (ارزان، بی‌ایجنت): consentPurpose-on-events → counters.
- **سیستم‌های فرایند (ساخته‌شده، برحسبِ‌تقاضا — نه دائمی):** نگهبانِ چندمرحله‌ای (`guardian-audit/review.workflow.js` — find→verify→fix→۲بازبین→حلقه؛ H1–H3 را گرفت) · **فورجِ جهانی** (`forge.workflow.js` — هر ایده/فیچر/طراحی/متن، shift-left، tier-dial) · خطِ رسپیِ کم‌هزینهٔ `author-batch-v2`.
- **توکن:** ~۴۷٪ هفتگی مصرف (بخشِ بزرگش misfireِ دستهٔ۴ بود)؛ انضباطِ بودجه فعال (بدونِ args، paced، tierِ سبک). نگهبان روی کارِ این نشست **هنوز نزده‌ام** (عمداً، بحرانِ بودجه) — وقتِ مناسب سبک می‌زنم.
- 🟢 **استانداردِ AI** آماده (`AI_STANDARD.md`) + ایده‌ها در `IDEAS_AND_GAPS.md`.

## ۶. تعریفِ «انجام‌شده» (تستِ پذیرش)
- «۱۰ خورش بپز → فردا بیشترِ خورش» قابلِ‌اثبات درست شود (حلقه ببندد).
- غذای سراسری‌رَدشده پایین برود، ولی برای اقلیتِ عاشقش بماند.
- منطقهٔ ماهی‌دوست پیش‌فرضِ دریایی بگیرد، بعد به فرد شخصی شود.
- کاربر «چون «فسنجان» را پختی» را ببیند و ذائقه‌اش را تنظیم کند.
- بتوانیم بگوییم «چرا کاربر X غذای برنامه‌اش را نپخت» و مداخله **حسِ مراقبت** بدهد نه سرکوفت.

---
*این دفتر با هر قدم به‌روز می‌شود. اگر چیزی اینجا نیست، یعنی هنوز ثبت نشده — بگو تا اضافه کنم.*

</div>
